{
  "name": "hmda-platform-api",
  "version": "0.0.0",
  "description": "Documentation for the HMDA Platform public API.",
  "main": "",
  "engines": {
    "node": ">=7.x"
  },
  "scripts": {
    "postinstall": "yarn run clearDist && yarn run build",
    "clearDist": "rimraf ./api ./api/js ./api/css ./api/fonts ./api/img && mkdirp ./api ./api/js ./api/css ./api/fonts ./api/img",
    "build": "yarn run sass && yarn run html && yarn run img && yarn run js",
    "html": "html-minifier --collapse-whitespace --file-ext html --input-dir ./src --output-dir ./api",
    "sass": "node-sass --quiet --output-style=compressed --source-map=./api/css/ ./src/sass/app.scss ./api/css/app.min.css && cp node_modules/uswds/src/fonts/sourcesanspro-* ./api/fonts && yarn run sass:prefix",
    "sass:prefix": "postcss -u autoprefixer -r ./api/css/*.css",
    "watch:sass": "node-sass --watch --quiet --output-style=compressed --source-map=./api/css/ ./src/sass/app.scss ./api/css/app.min.css",
    "img": "cp ./src/img/* ./api/img && cp -r ./node_modules/uswds/dist/img/* ./api/img",
    "js": "cp -r ./src/js/*.js ./api/js"
  },
  "repository": {
    "type": "git"
  },
  "keywords": [
    "HMDA",
    "API"
  ],
  "author": "Consumer Financial Protection Bureau",
  "license": "CC0-1.0",
  "bugs": {},
  "homepage": "",
  "devDependencies": {
    "autoprefixer": "^7.1.6",
    "mkdirp": "^0.5.1",
    "node-sass": "^4.7.2",
    "postcss-cli": "^4.1.1",
    "rimraf": "^2.6.2"
  },
  "dependencies": {
    "html-minifier": "^3.5.7",
    "uswds": "1.4.1"
  }
}
